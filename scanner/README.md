# Scanner Modules

The scanner is organized into focused modules for authentication, JWT analysis, BOLA/IDOR, SQL injection, XSS, parameter tampering, rate limiting, API key exposure, security headers, OWASP ZAP integration, and report generation.

Run the complete assessment from this directory with:

```bash
python security_scanner.py
```
